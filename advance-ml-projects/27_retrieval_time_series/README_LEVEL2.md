# Level-2 upgrade
The project now has a retrieval benchmark target: compare the original RAFT LSTM against a direct Transformer baseline and a retrieval-augmented Transformer under the same horizon/history and split. Retrieval corpora must contain training history only; never index validation/test windows.
