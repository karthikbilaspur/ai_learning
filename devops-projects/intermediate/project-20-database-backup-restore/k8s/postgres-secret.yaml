# For real use, generate this instead of committing it - see Project 17
# (Sealed Secrets or Vault) for how to avoid plaintext secrets in Git.
apiVersion: v1
kind: Secret
metadata:
  namespace: devops-starter
  name: postgres-credentials
type: Opaque
stringData:
  POSTGRES_USER: devops
  POSTGRES_PASSWORD: change-me-please
  POSTGRES_DB: devops_starter
